# hrequests-darwin-arm64

Platform-specific prebuilt bridge binary for [hrequests-js](https://github.com/JWriter20/hrequests-js).

Do not install this package directly — it is automatically pulled in as an optional dependency of `hrequests-js` when your platform matches.
